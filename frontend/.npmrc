## vite-plugin-pwa@1.2.0 declares peer vite@^3-7 but we use vite@8.
## The plugin works fine — the author just hasn't bumped the range yet.
## Remove this once vite-plugin-pwa releases with vite@8 support.
legacy-peer-deps=true
