# R10 Wave 5 — Mechanical Sweep + R10.5 Re-verification: Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Close every remaining R10 finding (dead code, docs, small quality items), re-baseline the Playwright e2e suite, complete the group-chat pending-state backend fix, and produce `FICINO_REVIEW_R10_5.md` — the campaign-closing re-verification of all 93 findings.

**Architecture:** Deletions and docs are mechanical (grep-gated); the e2e re-baseline updates specs to current product truth; the R10.5 pass re-runs the review discipline (verify against source, no speculation) over the full findings list.

**Tech Stack:** as prior waves. Baselines at start (post-wave-4 merge): api 186 / worker 33 / shared 33 / vitest 47 / eslint 0 / CI green.

## Global Constraints

- Resolves: WORK-10/11/14/15/16, API-9, API-17 (+BP-16 residue), DEP-4/5/7/9/10, BP-19, plus the accumulated cross-wave ledger items listed per task (each cites its origin). Requirement sources: `review/round10/*.md` + `.superpowers/sdd/progress.md` (the ledger).
- EVERY deletion is grep-gated: re-grep for consumers at HEAD before deleting; a consumer found = keep + report. Cite the grep in the report.
- Tests guarding deleted surface get retargeted, not deleted, where the review says so (API-9's signed-figure tests).
- TDD for the behavioral items (group-chat placeholder, reclaim-coupling assert); mechanical deletions ride the suites.
- Bisectability rules from waves 3-4 (def+use co-located; tsc/import smoke per commit).
- Commits end with: `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.
- Branch: `r10/wave5-sweep` off main (post-wave-4).

---

### Task 1: Backend dead-code deletions

**Files:** Delete `worker/lib/figure_describer.py` (WORK-10 — 183-line orphan; grep `figure_describer|describe_figure` first). Modify: `worker/tasks/preference_tasks.py` (WORK-11 — delete `get_preferences`), `worker/lib/pdf_extractor.py` (WORK-15 — delete `rasterize_page`; its only import was already removed in wave 1 — verify), `shared/ficino_shared/sanitize.py` + `worker/lib/sanitize.py` shim (WORK-16 — delete `fence_lines` IF still zero callers post-wave-2; grep both services), `worker/tasks/reading_list_tasks.py` + `api/routers/reading_lists.py` dispatch (WORK-14 — drop the unused `corpus_id` task param; check in-flight-task compat: keep a defaulted `_legacy=None` placeholder ONLY if the dispatch can't be updated atomically — both sides deploy together, so a clean drop is fine; note it), `api/routers/likes.py` (API-9 — delete `/likes/preferences` + `/likes/stats`), `api/routers/papers.py` (API-9 — delete `GET /papers/{id}/figures`; RETARGET `api/tests/test_signed_figures.py`'s three assertions at the live `/figures/{paper_id}/{figure_id}` serving route — read both routes first; the security property (signed-URL enforcement) must stay tested), `api/tests/test_likes.py` + `test_auth_scoping.py` (drop the tests that exist ONLY for the deleted endpoints — cite each), `api/constants.py` (API-17/BP-16 — delete `ENGAGEMENT_RANGES` + its false comment), `api/routers/personas.py` + `api/routers/replies.py` (API-17 residue — fix/remove the stale "feed_id is text" comments and the no-op `::uuid` casts IF the review's claim still holds: init.sql declares feed_id UUID; verify, then remove casts + comments; suite is the net).
- One commit per file-cluster (≈6 commits). Both container suites + shared suite green after each.

### Task 2: Dependency + docs sweep

**Files:** `worker/requirements.txt` (DEP-4 — remove `pydantic-settings`; verify no import: grep `pydantic_settings` in worker/ AND shared/; also confirm `pydantic` itself stays as documented-transitive with a comment), `api/config.py` (DEP-5 — delete `generation_limit_per_day`, `embed_dim`, `supabase_anon_key`, `elevenlabs_model_id` + any other zero-`settings.X`-read fields the finding lists; grep each; BP-6's characterization test guarding `generation_limit_per_day`'s existence must be UPDATED to assert its ABSENCE — cite the wave-3 test), `.env.example` (DEP-5: drop `GENERATION_LIMIT_PER_DAY` + the commented `SUPABASE_ANON_KEY`; DEP-9: ADD commented entries for `SIGNED_URL_KEY` (security-relevant), `POSTGRES_PASSWORD`/`REDIS_PASSWORD` with the DATABASE_URL/REDIS_URL coupling note, `RERANK_*`, `CONTEXT_*`, `FIGURE_DETECT_*`, `MAX_VISION_PAGES`, `ELEVENLABS_DIALOGUE_MODEL_ID`, `RETRIEVAL_*`, `CELERY_*_TIME_LIMIT`, `API_DB_POOL_*`/`WORKER_DB_POOL_*` — read each finding's var list + the wave-3 pool rename), root `package.json`... no — `frontend/package.json` (DEP-7 — remove `@playwright/test` from frontend devDeps; the root harness keeps its own; `npm ci` + build must stay green), `worker/backfill_metadata.py` (DEP-10 — add the "one-time script, run manually: docker compose exec worker python backfill_metadata.py" header), `FEATURES.md` (truth-up: group chats now creatable from the UI; stale-corpus alerts actually scheduled; anything the R10.5 pass flags as still-overstated gets a line), `docs/` or README (the wave-2 carried dev-setup note: bare-metal dev needs `pip install ./shared`), BP-19 (`logger.warn`→`logger.warning` repo-wide in api/+worker/ — mechanical sed + suite; add the one-line bind-convention comment in a worker task; leave backfill_metadata's print()s per its new header).
- ≈5 commits.

### Task 3: Small quality items (cross-wave ledger)

**Files/items (each cites its ledger origin):**
1. W3: `worker/tasks/audio_tasks.py` — derive the reclaim window from the env-tunable limit: `RECLAIM_MINUTES = max(15, (int(os.getenv("CELERY_TIME_LIMIT", "600")) * 2) // 60)` used in both claim SQLs via parameter (`NOW() - make_interval(mins => $N)` or interval multiplication — implementer picks the clean form), replacing the hardcoded 15; TDD: the existing claim tests parameterize.
2. W3: `api/routers/papers.py` — post-read oversize check returns 413 (was 400) matching the pre-check; update its test.
3. W3: `api/audit.py` — docstring warning: never call `record_audit` inside a `db.transaction()` block (a swallowed failed INSERT poisons the enclosing transaction).
4. W3: `worker/lib/event_loop.py` — dead-thread hardening: track the spawned thread; `_ensure_loop` re-creates when `thread.is_alive()` is false (not just loop.is_closed); extend the vitest— pytest test.
5. W4: `_shared/primitives.tsx` ApiKeyInput — a small "Clear" button (visible only when configured) wired to `onSave('')` with a confirm-style guard consistent with DangerButton's pattern; restores the revoke path lost in wave 4.
6. W4: `Settings/StorageTab.tsx` — `formatAgo(ts:number)` switches to shared `timeAgo(new Date(ts))` if output-compatible (compare formats first; keep if genuinely different, with a comment).
7. W4 R10.5 minors: `UserPostCard.tsx:103` `includes('404')` → `isNotFoundError`; `ListenView.tsx:419,456` `includes('501')` → `ApiError.status === 501`; user-facing raw `err.message` in NewGroupChatModal/GroupChatView/AccountTab → `getApiErrorDetail`; `useAlerts.ts` timer typing (`setInterval` handle); `usePollTask` controllers-Set entry removal on terminal dispatch.
- Frontend items: vitest/tsc/lint(0) hold; backend items: suites hold. ≈8 small commits.

### Task 4: Group-chat pending state (backend, from W4's ticket)

**Files:** `api/routers/messages.py` (`create_group_chat` inserts a placeholder `corpus_syntheses` row with `messages='[]', status='generating', task_id` — mirror `paper_summaries`' shape; read init.sql for the table's columns/constraints; if the table lacks a status/task_id column, add an additive migration mirroring paper_summaries' conventions + apply locally + CI list), `worker/tasks/summary_tasks.py` (`generate_corpus_synthesis`'s existing `ON CONFLICT (id) DO UPDATE` upsert must coexist with the placeholder — read the wave-era idempotency guard: it short-circuits on non-empty messages, so an empty placeholder passes; on retry-exhaustion set `status='error'` like paper summaries), `GET /messages/groups/{id}` (return the generating/error row with status instead of 404 — FRONTEND ALREADY HANDLES both: the retry-on-404 path becomes a fast-path status read; verify GroupChatView renders a `status:'generating'` response correctly — read its data handling; if it only expects complete shapes, the minimal contract is: generating → keep 404 semantics? NO — return `{status:'generating'}` and update GroupChatView's isDone to accept only complete; adjust the wave-4 spec's assertions if the pending detection changes). TDD end-to-end: api test seeding the placeholder + asserting the GET returns generating; the e2e spec updated if its 404-pending polling changes semantics.
- This is the one real feature-completing item; keep it minimal and mirror the paper_summaries precedent everywhere. ≈2 commits.

### Task 5: E2E suite re-baseline

**Scope:** the full Playwright suite currently has ~77 drift failures (wave-1 gate triage: selectors/copy written against an older UI — h1-vs-h2, `aria-label="Alerts, N unread"`, logo alt, desktop-only boot() in the mobile project, SW-on-self-signed-cert) plus the round9 allow-list flake (missing 'pending'). Deliverable: **the full suite runs green locally** against the current product, and the flaky/impossible specs are handled explicitly.
- [ ] Step 1: full run (`npx playwright test --reporter=line` with the working E2E_BASE_URL), bucket every failure: (a) assertion drift → UPDATE the spec to current truth (the product is correct; the spec is stale — each update cites what changed); (b) structurally obsolete (tests a UI that no longer exists) → DELETE with a one-line justification; (c) environment-impossible locally (SW/self-signed) → mark `test.skip` with a reason string naming the env constraint; (d) genuine product bug discovered → STOP on that spec and report it to the controller (do not paper over).
- [ ] Step 2: fix the round9 'pending' allow-list; de-flake anything with raw waits where a condition-wait is available.
- [ ] Step 3: three consecutive full-suite green runs (flake check). Report the bucket table (spec → disposition → evidence).
- ≈3 commits (updates / deletions / skips+flake fixes). NOTE: this task may surface product bugs — budget for controller escalation.

### Task 6: R10.5 — re-verify all 93 findings + campaign close-out

Fan out verification agents over `FICINO_REVIEW_R10.md`'s 93 merged findings (the controller orchestrates — same no-speculation discipline as the original review: every status verified against HEAD source with file:line evidence). Output `FICINO_REVIEW_R10_5.md` at repo root:
1. Status table: fixed (with the fixing commit/wave) / deferred-with-reason / superseded — for all 93.
2. The cross-wave ledger's deferred minors: each either closed by Tasks 1-5 or listed under "Known deferrals" with rationale.
3. A "new findings during remediation" section: everything the review loops caught that wasn't in R10 (the settings launder loop, the beat reset, the FE-2 regression, etc.) — status of each.
4. Campaign metrics: waves/commits/tests-added/suite counts before-after.
Commit the report. Also update `FEATURES.md`/docs if the verification finds drift Task 2 missed.

### Task 7: Wave gate + final review (controller)

Suites (api/worker/shared/vitest/lint) + full e2e green (now meaningful post-re-baseline!) + browser smoke + push/PR/CI green → whole-branch final review (strongest model) → fix loop → Ready.

### Task 8: Merge + deploy verify + campaign close (controller)

Merge; watch CI + all three Railway deploys (this wave touches api, worker, AND frontend); healthz + celery + beat banner; prod browser smoke; if Task 4 added a migration, it precedes the merge (user authorization required for prod DDL — request it explicitly BEFORE the merge window). Delete branch; final ledger + memory close-out; the goal condition (waves 4+5 complete) is met here.
