# Development-only dependencies (pytest + client libs for tests).
# Install inside the api container: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.28.0
