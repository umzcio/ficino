# Round 10.5 Verification — Agent: `api/` (Round 10 findings API-1 … API-20)

- **Verified against**: HEAD `764777c` (branch `r10/wave5-sweep`), all five remediation waves present.
- **Method**: read the current source at each finding's cited location(s), confirmed behavior directly (not inferred from commit messages), then used `git log -S`/`git log --oneline <range>` plus `.superpowers/sdd/progress.md` to attribute the fixing wave/task. Every FIXED verdict below cites the current file:line I read.

## Findings

| ID | Status | Evidence (file:line) | Wave | Notes |
|----|--------|------------------------|------|-------|
| API-1 | **FIXED** | api/routers/messages.py:139-141 — `if summary and (summary["status"] or "complete") == "error": ... summary = None` drops error rows so they fall through to the dispatch/re-generate branch at :184-197. Worker comment at worker/tasks/summary_tasks.py no longer claims false handling. | Wave 1, Task 6 (commit `96e72a5`, "R10 API-1, R9 H13") | Frontend still has no explicit retry button, but it no longer matters — a fresh GET auto-redispatches instead of returning the stale error forever. |
| API-2 | **FIXED** | api/routers/messages.py:111-119,184 — `Depends(RateLimit(...))` removed from the handler signature; rate limit now charged imperatively via `await check_rate_limit(user, "summary", ...)` immediately before `send_task`, after the cached-read return at :162-177. | Wave 3, Task 8 (commit `6094745`, "R10 API-2") | New shared `check_rate_limit` helper (factored out of `RateLimit.__call__`) used here. |
| API-3 | **FIXED** | api/routers/feed.py:160-219 — `_hydrate_audio_urls`/`_hydrate_podcast_episode_url` batch all per-post signed-URL calls into one `await asyncio.to_thread(_fetch_all)` / `asyncio.to_thread(_fetch)` hop instead of N blocking calls on the event loop. Same pattern applied to `list_figures` in api/routers/papers.py:307-317 (`_sign_all` + one `asyncio.to_thread`). | Wave 3, Task 9 (commit `8059676`) | Comment explicitly cross-references "R10 API-3" at both sites. |
| API-4 | **FIXED** | api/models/user.py:6-14 — `default_corpus_id` dropped from `UserUpdate` with a documented rationale (not implemented, avoids a second source of truth). frontend/src/lib/api.ts:392-414 + components/Settings/AccountTab.tsx:6,41 — `GET /users/me` and `GET /users/me/audit-log` now have real frontend callers (`getMe()`, `listAuditLog()`). | Wave 3, Task 12 (commit `23769cd`, default_corpus_id) + Wave 4, Task 9 (commit `b73339b`, frontend wiring) | Residual: `PUT /users/me` (`update_user_profile`) still has zero callers anywhere (frontend or tests) — grepped `api/tests/` and `frontend/src/` for `users/me` + PUT, no hits. Not called out in the ledger; a small leftover gap versus "all three endpoints" being live. |
| API-5 | **FIXED** | api/celery_client.py (new file) — single module-level `celery_app = Celery(broker=..., backend=...)` + `get_celery()`. All five prior per-router `_get_celery`/inline `Celery(...)` copies (feed.py, messages.py, reading_lists.py, user_posts.py, papers.py) replaced with `from celery_client import get_celery`. | Wave 3, Task 7 (commit `e001468`, "R10 API-5/DUP-13/BP-15") | Module docstring explicitly notes it fixes the papers.py backend-omission drift the original finding called out. |
| API-6 | **FIXED** | api/routers/personas.py:22-25 — `list_personas` now requires `user: AuthUser = Depends(get_current_user)`. api/routers/settings.py:192-193 — `list_ollama_models` now requires the same. Both handlers carry inline comments citing "R10 API-6/BP-13". | Wave 3, Task 7 (commit `8ddaf0c`) | |
| API-7 | **FIXED** | `ls api/models/` shows only feed.py, paper.py, requests.py, user.py, `__init__.py` remain; `grep -rn "class Corpus\|class User\b\|class PaperCreate\|class Chunk\b" api/models/` returns nothing. `api/tests/test_models.py` deleted (verified: file does not exist). | Wave 3, Task 11 (commit `946dfc0`, "R10 BP-4/API-7 dead half") | |
| API-8 | **FIXED** | api/config.py — all 9 originally-flagged dead fields absent: `embed_provider`, `ollama_embed_model`, `ollama_vision_model`, `embed_dim`, `openai_api_key`, `voyage_api_key`, `supabase_anon_key`, `elevenlabs_model_id`, `generation_limit_per_day` all gone (grepped, zero hits). Comment at config.py:21-28,56-59 documents the deletion and where the real per-user knobs now live. | Wave 5, Task 2 (commit `d646791`, "R10 DEP-5, DEP-9") | **Surprise (new, not a regression)**: the same wave's review found 4 *more* dead-as-attribute fields not in the original API-8 list — `llm_provider`, `claude_model`, `ollama_llm_model`, `anthropic_api_key` (still present at config.py:11,15,18,19, confirmed zero `settings.X` reads in api/) — deliberately left per scope discipline per progress.md ("W5 Task 2... confirmed 0-read by both implementer + reviewer but left per scope discipline — R10.5 deferral, documented in-code"). This is a new finding outside API-8's original scope, not a regression of API-8 itself. |
| API-9 | **PARTIAL (2/3)** | api/routers/likes.py — `grep -n "preferences\|stats"` returns nothing; both dead endpoints deleted. api/routers/papers.py:289-325 — `GET /{paper_id}/figures` (`list_figures`) still present, now with the API-3 batching fix applied too. api/tests/test_signed_figures.py:131-133 still calls it directly. | Wave 5, Task 1 (commit `5e908d7`, "R10 API-9, partial") | Matches the ledger's own framing exactly: kept deliberately as "a genuine wave-3 test consumer (off-loop hydration)," not an oversight. |
| API-10 | **FIXED** | api/routers/likes.py:65-73 and api/routers/bookmarks.py:72-85 — both now do a single `INSERT ... ON CONFLICT (user_id, feed_id, post_index, message_index) DO NOTHING RETURNING id`, replacing the check-then-insert race. Comments cite the NOT NULL DEFAULT -1 invariant that makes the unique constraint airtight against NULLs. | Wave 3, Task 8 (commit `73212b2`, "R10 API-10") | |
| API-11 | **FIXED** | api/routers/citations.py:34-43 — `formatted` now built from ALL authors (not `authors[:19]`); >20-author branch is `", ".join(formatted[:19]) + ", ... " + formatted[-1]` where `formatted[-1]` is now the true last author; ≤20 branch joins everyone with `&` (no longer silently drops author #20). | Wave 3, Task 8 (commit `f7ac371`) | |
| API-12 | **FIXED** | api/routers/reading_lists.py:275-278 (`reorder_reading_list`) and :356 (`apply_ai_ordering`) both add `len(sequence) != len(set(sequence))` → 422 checks before the existing permutation/ownership checks. `apply_ai_ordering`'s `body` param is now `ApplyOrderingRequest` (pydantic model, not raw `dict`) per its docstring at reading_lists.py:326-330. | Wave 3, Task 8 (commit `588c798`) | |
| API-13 | **FIXED** | api/routers/workspaces.py:28-31 — dedicated `_activity_sort_key` helper: `return activity["timestamp"] or datetime.min.replace(tzinfo=timezone.utc)`, used at :245 (`activities.sort(key=_activity_sort_key, reverse=True)`). No more str/datetime mix. | Wave 3, Task 8 (commit `d1a308a`) | |
| API-14 | **FIXED** | api/routers/settings.py:386-390 — `clear_all_papers` now wraps both deletes in `async with db.transaction(): DELETE FROM papers ...; DELETE FROM feeds ...`. `_cleanup_artifacts` is a module-level function shared by `clear_everything` and `clear_all_papers` (both call sites at settings.py invoke the same helper name). | Wave 3, Task 8 (commit `29d324f`) | |
| API-15 | **FIXED** | api/routers/user_posts.py — `create_user_post`'s `send_task` is now wrapped in try/except; on dispatch failure it sets `status = 'error'` and raises 503 instead of stranding the row (comment cites "R10 API-15"). `reply_to_user_post`'s follow-up UPDATE now reads `UPDATE user_posts SET replies = ..., status = 'pending' WHERE id = $2 AND status = 'complete' RETURNING id`, returning 409 when 0 rows match — closes the concurrent-double-append race. | Wave 3, Task 8 (commit `8b41650`) | |
| API-16 | **FIXED** | api/routers/personas.py:289-296 (docstring) + implementation — `delete_persona_dm_message` now uses atomic `messages - $1::int` JSONB element removal, same pattern as replies.py's `delete_reply_message`, replacing the old SELECT→pop→overwrite race. | Wave 3, Task 8 (commit `f2799b0`) | |
| API-17 | **FIXED** | `grep -n "ENGAGEMENT_RANGES" api/constants.py` returns nothing (only worker/tasks/persona_tasks.py:39 keeps its own copy, which is correct/expected since containers can't share the import). `grep -n "feed_id::uuid\|feed_id is stored as text" api/routers/personas.py api/routers/replies.py` returns nothing — the stale comment and no-op casts are gone. | Wave 5, Task 1 (commit `72cf6b7`, "R10 API-17, BP-16") | |
| API-18 | **PARTIAL** | Fixed: `api/routers/papers.py` — dead `_get_redis`/`from redis import Redis` deleted (verified: zero hits). `api/main.py` — no dead `get_db` import remains. `api/routers/personas.py`/`replies.py` — `settings` import is now used (`RateLimit(..., settings.rate_limit_*)`). `ruff check .` on `api/` → "All checks passed!". | Wave 3, Task 7 (commit `e001468`, redis) + Wave 1, Task 10 (commit `64053d7`, "R10 DEP-8" ruff/CI cleanup) | Residual, NOT closed: the unused `request: Request` params in `request_feed_audio` (feed.py:257) and `request_feed_podcast` (feed.py:297) are still present and still unused (no `record_audit` call added in either handler) — confirmed by direct read. Ruff's default rule set doesn't flag unused function parameters (only F401/F841), so this slipped through the "ruff-verified" cleanup. |
| API-19 | **FIXED** | api/routers/papers.py:66-97 — Content-Length pre-check added (`declared_size > max_bytes + 16384` → 413) before `await file.read()`; the post-read `len(contents) > max_bytes` check now also returns 413 (aligned with the pre-check) instead of 400. | Wave 3 (commit `de5903a`, pre-check) + Wave 5, Task 3 (commit `540f064`, 413-vs-400 alignment follow-up) | |
| API-20 | **PARTIAL** | api/routers/replies.py:386-398 (`create_reply`) and :746-756 (`zap_response`) both still assemble `chunks_text` via an identical `"\n\n".join(f"[{c['section']}]\n{fence_untrusted(str(c['content']))}" for c in grounded_chunks)` after calling the shared `_load_reply_context_chunks`. | Incidentally improved, Wave 3 Task 11 (commit `520c06c`, "R10 DUP-17") | The bulk of the originally-flagged ~26-line duplicate (fetch feed row, JSON-parse, bounds-check `post_index`, extract/filter `sources`) WAS extracted, into `_load_conversation_and_sources` — but that was tracked as a separate finding (DUP-17), not filed as API-20. `_load_reply_context_chunks` itself long predates R10 (introduced at `55de893`, already shared before the review). No commit or ledger entry closes API-20 by name; the ~8-line `chunks_text` assembly the recommendation asked to fold into one combined helper (`_load_grounding(...) -> tuple[chunks, chunks_text]`) is still duplicated verbatim in both handlers. Effectively DEFERRED as its own ticket, with incidental partial relief from an adjacent fix. |

## Regressions/Surprises

- **No regressions found.** Every finding that was reported fixed by the wave ledger was re-verified directly against current source and holds.
- **Surprise (API-8 adjacent, not itself a regression)**: 4 more dead `Settings` fields (`llm_provider`, `claude_model`, `ollama_llm_model`, `anthropic_api_key`) discovered during Wave 5 sweep, confirmed still present and still zero-read in `api/config.py:11,15,18,19` at HEAD. Deliberately deferred per `progress.md`'s W5 Task 2 note, not a miss — flagging for whoever owns the next dead-code pass.
- **Two items left genuinely open, contrary to a naive "all closed" read of the ledger**: API-4's `PUT /users/me` remains an uncalled endpoint (2 of 3 users.py endpoints now have callers, not all 3), and API-20 was never closed as its own item — only incidentally shrunk by the unrelated DUP-17 extraction. Neither is called out explicitly as still-open anywhere in `progress.md`, so worth flagging for R10.5 planning rather than assuming done.
- API-18's unused `request: Request` params in `feed.py:257,297` are a small, still-open loose end from an otherwise-complete cleanup (everything else in that finding — imports, dead redis client — is gone).

**Totals: 15 FIXED, 4 PARTIAL (API-4, API-9, API-18, API-20), 0 DEFERRED (untouched), 0 SUPERSEDED, 0 REGRESSED.**
