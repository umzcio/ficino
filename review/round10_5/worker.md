# Round 10.5 — Worker Verification (`worker/`)

Verified against HEAD `764777c` (branch `r10/wave5-sweep`, all 5 waves present). Every row below is a
direct read of current source — no speculation. Wave attribution is via commit-message `R10 WORK-N`
tags cross-referenced against `.superpowers/sdd/progress.md`'s per-task commit lists.

| ID | Status | Evidence (file:line) | Wave | Notes |
|----|--------|------------------------|------|-------|
| WORK-1 | FIXED | `worker/tasks/ingestion_tasks.py:23` now imports `fetch` (`from lib.db import execute, fetchrow, store_chunks_batch, store_figure` → actual line reads `from lib.db import execute, fetch, fetchrow, store_chunks_batch, store_figure`); call site `worker/tasks/ingestion_tasks.py:204` `tag_rows = fetch(...)`. `ruff check worker/` → "All checks passed!" (no F821). | Wave 1 (commit `674b872`) | Auto-tagging restored. |
| WORK-2 | FIXED | `worker/tasks/audio_tasks.py:38` imports `apply_provider_settings`; `worker/tasks/audio_tasks.py:398` calls `apply_provider_settings(user_id)` immediately after the podcast claim (`user_id = str(claimed["user_id"])` at ~L392), before `build_podcast_script()` runs retrieval/LLM. In-code comment cites "R10 WORK-2" directly. | Wave 1 (commit `b61ff36`) | Matches Round-9 C1-C4 pattern exactly. |
| WORK-3 | FIXED | `worker/lib/settings.py:163-170` (`apply_provider_settings`): for every `SETTINGS_TO_ENV` key, if the user's explicit value is falsy it now does `_active_settings.pop(setting_key, None)` and restores `baseline_env().get(env_var)` into `os.environ` (or pops the var if no baseline) — no more stale-key leakage between users. Comment explicitly: "restore the operator baseline instead of leaving the previous user's value behind (R10 WORK-3)." | Wave 1 (commits `3c77511`, `4bb91a3`; final-review hardening `554e091` — "reassert from operator baseline, not live env, under PUBLIC_DEPLOYMENT") | Went through 2 follow-up rounds before landing; current code reads baseline from `baseline_env()`, not live env, closing the PUBLIC_DEPLOYMENT re-laundering path the first attempt missed. |
| WORK-4 | FIXED | `worker/tasks/reading_list_tasks.py:20` imports `validate_post_shape` from `lib.post_validation`; `worker/tasks/reading_list_tasks.py:391` calls `validate_post_shape(post_data, persona_key=persona_key)` before `posts.append(post_data)` at L393, inside the same try/except that drops the slot on failure. | Wave 1 (commit `bc29b1a`) | Closes both this finding and the parent Round-9 #27. |
| WORK-5 | FIXED | Claim queries now reclaim stale rows: `worker/tasks/audio_tasks.py:210-216` (feed audio) and `:380-386` (podcast) both add `OR (audio_status/podcast_status = 'generating' AND (claimed_at IS NULL OR claimed_at < NOW() - make_interval(mins => $2)))`, driven by `_reclaim_minutes()` (`audio_tasks.py:57-71`, `return max(15, (int(os.getenv("CELERY_TIME_LIMIT", "600")) * 2) // 60)`). Migration `infra/postgres/add_feed_media_claimed_at.sql` adds `audio_claimed_at`/`podcast_claimed_at` columns (confirmed applied to prod per progress.md W3 Task 14). | Wave 3 (commit `9e287cc`), reclaim-window formula refined in Wave 5 (commit `ea027f0`, "derive audio/podcast reclaim window from CELERY_TIME_LIMIT") | Two-stage fix: wave 3 shipped a fixed window, wave 5 tied it to the actual task time limit. |
| WORK-6 | FIXED | `worker/celery_app.py:52-62`: `beat_schedule={"check-stale-papers-daily": {"task": "tasks.alert_tasks.check_stale_papers", "schedule": crontab(hour=3, minute=0), ...}}`. Query bug also fixed: `worker/tasks/alert_tasks.py:296-302` now scopes `WHERE f.user_id = p.user_id AND (f.corpus_id = p.corpus_id OR f.corpus_id IS NULL)` — the `OR f.corpus_id IS NULL` closes the NULL-corpus-feed gap the finding flagged, and `f.user_id = p.user_id` closes an additional cross-tenant scoping bug the fix also caught. | Wave 3 (commit `9dd4d07`; crontab-vs-interval final-review fix `e571818`) | Beat runs embedded in the worker process per `celery_app.py` comment — correct only for single-replica deployment, called out in-code. |
| WORK-7 | FIXED | `worker/tasks/reading_list_tasks.py:418-425`: after the `feeds` upsert, a try/except-non-fatal block imports `_write_feed_posts_index` from `tasks.persona_tasks` and calls `_write_feed_posts_index(feed_id, posts, 0, effective_user_id)`, matching the pattern in `generate_feed`/`regenerate_post`. | Wave 3 (commit `30ea53a`) | Chapter posts now searchable via `feed_posts`. |
| WORK-8 | FIXED | New helper `worker/tasks/summary_tasks.py:17-34` `_coerce_messages(parsed)` filters to `[m for m in parsed if isinstance(m, dict) and isinstance(m.get("content"), str) and m.get("content")]`; called at both parse sites — `summary_tasks.py:199` (paper summary) and `:341` (synthesis) — before the existing `if not messages:` empty-fallback, so a wrong-shaped (e.g., array-of-strings) response now falls through to the single-bubble fallback instead of persisting malformed elements. | Wave 3 (commit `a06a5e2`) | |
| WORK-9 | FIXED | `worker/lib/vision_extractor.py:77-106` (`_extract_page_claude`) and `:132-166` (`_extract_page_ollama`) both wrap their calls in a 3-attempt loop with `wait = 2 * (3 ** attempt)` (2s/6s/18s) backoff, catching connection/timeout/5xx/rate-limit errors; Claude path pins SDK `max_retries=0` so the outer loop is the sole retry owner (avoiding retry-stacking, per in-code comment referencing the api-side H30 pin). | Wave 3 (commits `076ca86`, `70c1cb6` retry-stacking fix, `2e70b97` test-recorder follow-up) | |
| WORK-10 | FIXED (module deleted) | `worker/lib/figure_describer.py` no longer exists (`ls`: "No such file or directory"). Repo-wide grep for `figure_describer`/`describe_figure` returns zero hits. | Wave 5 (commit `a9fc47d`, dead-code sweep W5 Task 1) | |
| WORK-11 | FIXED (function deleted) | `worker/tasks/preference_tasks.py` no longer defines `get_preferences` — `grep -n "def "` shows only `compute_preferences`, `_normalize`, `_get_liked_paper_titles`, `_store_preferences`. Zero repo-wide callers remained pre-deletion per the original finding. | Wave 5 (commit `ec15e12`) | |
| WORK-12 | FIXED | `ruff check worker/` → "All checks passed!" — zero F401/F541 findings; all 13 named unused imports (`Counter`, `retrieve_chunks`, f-string, `STUB_USER_ID`, `os`, `asyncio`, `uuid`, `Path`, `json`, `rasterize_page`, etc.) are gone from their respective files. | Wave 1 (commit `64053d7`, "clear F-class lint debt (R10 DEP-8)", part of the CI-add task) | Also the commit that put `ruff` into CI, per progress.md Task 10 — prevents regression. |
| WORK-13 | FIXED (methods deleted; module now a shim) | `grep -rn "save_podcast_segment\|podcast_segment_url\|_podcast_segment_key" worker/lib/storage.py` → no hits. `worker/lib/storage.py:1` is now `"""Shim — backends live in ficino_shared.storage (R10 DUP-2). Env-wired."""`, re-exporting `StorageBackend`/`build_backend` from `ficino_shared.storage`; `grep -rn "podcast_segment" shared/ficino_shared/` → no hits, confirming the consolidated backend never carried the dead methods forward. | Wave 2 (commit `684b30d`, tagged "R10 DUP-2, WORK-13") | SUPERSEDED in the structural sense: `worker/lib/storage.py` itself is no longer an independent implementation — both worker and api storage now delegate to one `ficino_shared.storage` module (Wave 2's storage-consolidation effort), and the dead segment methods were dropped in the same move rather than patched in place. |
| WORK-14 | FIXED | `worker/tasks/reading_list_tasks.py:50-55` — `propose_ordering(self, paper_ids, list_id=None, user_id=None)` — no `corpus_id` parameter. `api/routers/reading_lists.py:238-241` dispatch: `args=[paper_ids, list_id], kwargs={"user_id": user.id}` — no `corpus_id` passed. | Wave 5 (commit `a83bf27`) | |
| WORK-15 | FIXED (function deleted) | `grep -n "^def rasterize\|^async def rasterize" worker/lib/pdf_extractor.py` → only `rasterize_pages` (plural, the streaming path) remains; no single-page `rasterize_page` definition. `worker/lib/vision_extractor.py:210` retains only an explanatory comment referencing the old name, no import. | Wave 5 (commit `3483532`) | |
| WORK-16 | FIXED (function deleted, module consolidated) | `worker/lib/sanitize.py` is now a 7-line shim: `"""Shim — the implementation lives in ficino_shared.sanitize (R10 DUP-3)."""` re-exporting `fence_untrusted`, `sanitize_inline`, `strip_role_markers` only. `grep -n "^def " shared/ficino_shared/sanitize.py` confirms `fence_lines` was not carried forward into the shared module — it was dropped, not moved. | Wave 5 (commit `903050a`) | SUPERSEDED structurally: worker's `sanitize.py` is now a thin re-export of `ficino_shared.sanitize` (Wave 2/3-era DUP-3 consolidation); the dead `fence_lines` helper was deleted outright during the Wave-5 sweep rather than migrated. |
| WORK-17 | FIXED | `worker/lib/persona.py:645-661` — new `eligible_persona_keys(enabled_personas)` helper: empty/None `enabled_personas` now returns `[k for k, meta in PERSONAS.items() if meta.get("feed_eligible")]` instead of all `PERSONAS.keys()`. `plan_feed_posts` (`persona.py:679`) calls this helper. In-code comment: "let reply-only personas like the Archivist ... author feed posts whenever a user opted out of every persona (R10 WORK-17)." | Wave 3 (commit `3d388c0`) | |
| WORK-18 | FIXED | `worker/lib/metadata_extractor.py:127` — `_runner = LoopRunner("meta-loop")`; `_run_on_meta_loop` (`:130-131`) now delegates to `_runner.run(coro)`. `worker/lib/event_loop.py` defines the shared `LoopRunner` class: dedicated daemon thread running `loop.run_forever()`, submission via `asyncio.run_coroutine_threadsafe(...).result()` — the same daemon-thread pattern used by db/embedder/claude_client/vision_extractor/figure_detector/contextualizer. In-code comment cites the exact bug: "this module previously ran `with _lock: loop.run_until_complete(coro)` on the *caller's* thread ... serialized every metadata extraction ... (R10 DUP-5)." | Wave 3 (commit `0219ab4`, "shared LoopRunner for background event loops ... (R10 DUP-5)") | SUPERSEDED: rather than a local port of the daemon-loop pattern (as the finding recommended), the fix introduced a single shared `LoopRunner` abstraction (`worker/lib/event_loop.py`) that `metadata_extractor` (and presumably other modules) now use — broader refactor than the finding's literal ask, but resolves it. `LoopRunner` itself got a later dead-thread-recreation hardening fix (Wave 5, commit `67d02a1`) unrelated to this finding but worth noting for completeness. |

## Regressions/Surprises

None found. All 18 WORK-1 through WORK-18 findings are confirmed FIXED at HEAD with no regressions.
One item worth flagging as a **process observation, not a regression**: WORK-3 required three
separate commits across Wave 1 (`3c77511` → `4bb91a3` → final-review `554e091`) before the fix was
fully correct — the first two passes still allowed the operator-baseline value to be re-laundered
through `os.environ` under `PUBLIC_DEPLOYMENT`. Current code (`settings.py:163-170`) closes that gap
by reading from `baseline_env()` rather than live env, and this was independently re-verified here by
reading current source, not by trusting the commit log.

Two items (WORK-13, WORK-16, WORK-18) are more accurately **SUPERSEDED** than plain FIXED: the
original module (`worker/lib/storage.py`, `worker/lib/sanitize.py`) was replaced by a shim over a
newly consolidated `shared/ficino_shared` module as part of Wave 2/3 dedup work, and WORK-18's fix
introduced a new shared abstraction (`LoopRunner`) rather than a local patch. Functionally these are
all still resolved — flagged here only so the wave-2/3 consolidation isn't mistaken for coincidental
side-effects.
