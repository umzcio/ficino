# R10.5 — Dependency/Deploy Hygiene Re-verification

Branch `r10/wave5-sweep` @ `764777c` (all five waves present). Every row below was
independently re-verified against HEAD source (greps/reads/tool runs), not taken on
the strength of the wave report — commit hashes are cited for traceability, not as
proof.

| ID | Status | Evidence (file:line) | Wave | Notes |
|----|--------|----------------------|------|-------|
| DEP-1 | FIXED | `worker/tasks/ingestion_tasks.py:23` — `from lib.db import execute, fetch, fetchrow, store_chunks_batch, store_figure` (`fetch` now imported). Call site `worker/tasks/ingestion_tasks.py:204` (`tag_rows = fetch(...)`) resolves. Except clause `worker/tasks/ingestion_tasks.py:220-223` now `log.error("auto_tag_failed", ..., error_type=type(e).__name__)` (was silent `.warn`). | Wave 1 (commit `674b872`, in main) | Auto-tagging batched INSERT (tags + paper_tags, ON CONFLICT) now reachable; NameError class of bug would surface as `.error` with `error_type`, not a swallowed warn. |
| DEP-2 | FIXED | `frontend/package.json:17` — `"idb": "^7.1.1"` now declared directly. Root `/projects/ficino/package.json` — grep for `idb` returns 0 hits (unused v8 pin removed). | Wave 1 (commit `f275a6a`, in main) | Version pinned matches what tsc actually compiles against (v7, per the wave-1 plan's deliberate choice over an out-of-scope v8 upgrade). |
| DEP-3 | FIXED | `worker/lib/figure_describer.py` — file absent (`ls` → No such file or directory). `grep -rn "figure_describer\|describe_figure" --include=*.py .` (repo-wide, excl. `__pycache__`) → 0 hits. | Wave 5 Task 1 (commit `a9fc47d`, on `r10/wave5-sweep`, not yet merged to main) | Orphan 183-line module deleted outright, as recommended. |
| DEP-4 | FIXED | `worker/requirements.txt:10-15` — `pydantic-settings` line removed; `pydantic>=2.9.0` kept with an added comment: "Transitive via anthropic/openai SDKs — no direct `import pydantic` in worker code... Pin kept as a floor...". `grep -rn "pydantic" worker/ shared/ --include=*.py` (excl. `__pycache__`) → 0 hits confirming no import. | Wave 5 Task 2 (commit `e49a50c`, on `r10/wave5-sweep`) | Matches recommendation exactly (remove pydantic-settings, keep pydantic pin with rationale comment). |
| DEP-5 | FIXED | `api/config.py:56` and `:80-81` — `generation_limit_per_day`, `embed_dim`, `supabase_anon_key`, `elevenlabs_model_id` all removed from the Settings class, each replaced by an explanatory comment. `grep -rn "generation_limit_per_day\|embed_dim\b\|supabase_anon_key\|elevenlabs_model_id" api/ --include=*.py` → only the comment/test lines remain (`api/config.py:56,80-81`, `api/tests/test_rate_limit_config.py:7,27-33`). `.env.example:60` — `GENERATION_LIMIT_PER_DAY` line removed, replaced by a note that the browser key is `VITE_SUPABASE_ANON_KEY`. | Wave 5 Task 2 (commit `d646791`, on `r10/wave5-sweep`) | `test_rate_limit_config.py` was updated (not just deleted): old `test_generation_limit_per_day_untouched` replaced with `test_generation_limit_per_day_removed` asserting `not hasattr(s, "generation_limit_per_day")` — correctly flips the characterization test's polarity. Sweep also removed 5 more zero-read Settings mirrors (`embed_provider`, `ollama_embed_model`, `ollama_vision_model`, `openai_api_key`, `voyage_api_key`) beyond the finding's literal four fields — scope expansion, not a defect. |
| DEP-6 | FIXED | `frontend/src/lib/api.ts` — `grep -n "getPaper\b\|applyReadingListOrdering\|listTags\|createTag\|deleteTag"` → 0 defining hits (all six wrappers gone; `assignTag`/`unassignTag` remain at `api.ts:685,693`, tag CRUD's live path). `frontend/src/lib/offline-cache.ts` — `networkFirst` absent (0 hits). `frontend/src/types/index.ts` — `PersonaData` re-export absent (0 hits over full 154-line file); `PersonaData` itself still lives at `api.ts:300` and is imported directly by consumers. `createGroupChat` (`api.ts:719`) now HAS a caller: `frontend/src/components/Messages/NewGroupChatModal.tsx:3,79`. | Wave 4 (commits `be508bd` "delete dead exports flagged by R10 FE-11", `41a5d27` "wire up group-chat creation modal R10 FE-4" — both in main) | Handled exactly per the recommendation's conditional: the five genuinely-dead wrappers + `networkFirst` + the re-export were deleted, and `createGroupChat` was made live (group-chat creation UI shipped) rather than deleted — resolving the "product gap" the finding flagged as needing deliberate triage. |
| DEP-7 | FIXED | `frontend/package.json` — `grep -n "playwright"` → 0 hits (devDependency removed). Root `/projects/ficino/package.json`'s own `@playwright/test` + `playwright.config.ts` untouched (unaffected, verified by wave-5 report's gate log). | Wave 5 Task 2 (commit `fc484bf`, on `r10/wave5-sweep`) | Wave-5 report cites a clean `npm install` (3 packages removed) → `tsc -b` → `npm run build` → `npm test` (47 passed) → `npm run lint` (0) gate; re-ran `npm run lint` myself at HEAD, still 0 problems (see DEP-8 row). |
| DEP-8 | FIXED | `ruff check .` re-run by me at HEAD in `api/`, `worker/`, `shared/` → **All checks passed!** in all three. `cd frontend && npm run lint` re-run by me at HEAD → 0 problems. CI wiring: `.github/workflows/ci.yml:9-17` (`python-lint` job: `pip install ruff` + `ruff check api/ worker/ shared/`) and `:188` (`cd frontend && npm run lint`) both present. | Wave 1 (CI + initial F-class fixes, commit range ending `554e091`/`c0aacf7`) + Wave 4 (`npm run lint` 21→0, commit `7c671ed` per progress ledger) | Both halves of the recommendation delivered: the F-class debt was cleared AND a ruff config + CI gate now exist so it can't silently regrow (`ruff.toml`/pyproject ruff section present in api/worker/shared, confirmed by `ruff check .` running without "no config" warnings). |
| DEP-9 | FIXED | `.env.example` — every var named in the finding is now present (commented, matching file convention): `SIGNED_URL_KEY:105`, `POSTGRES_PASSWORD:164`, `REDIS_PASSWORD:165` (with an explicit coupling note at `:161-162` that DATABASE_URL/REDIS_URL must be updated to match, since compose won't do it automatically), `RERANK_PROVIDER/_LOCAL_MODEL/_VOYAGE_MODEL/_COHERE_MODEL:117-120`, `CONTEXT_PROVIDER/_ANTHROPIC_MODEL/_OLLAMA_MODEL/_MAX_PAPER_CHARS/_MAX_TOKENS:125-129`, `FIGURE_DETECT_PROVIDER:132`, `MAX_VISION_PAGES:135`, `ELEVENLABS_DIALOGUE_MODEL_ID:139`, `RETRIEVAL_CANDIDATE_POOL/_RERANK_MULTIPLIER:142-143`, `CELERY_SOFT_TIME_LIMIT/_TIME_LIMIT:147-148`, `COOKIE_DOMAIN:100`, `SEARCH_USE_NORMALIZED_POSTS:109`, `RATE_LIMIT_SUMMARY_PER_DAY:113`. | Wave 5 Task 2 (commit `d646791`, on `r10/wave5-sweep`) | Sweep also added `API_DB_POOL_MIN_SIZE/_MAX_SIZE:153-154` and `WORKER_DB_POOL_MIN_SIZE/_MAX_SIZE:155-156` — the wave-3 renamed pool vars, not literally named in the original DEP-9 text (which predated the rename) but clearly in scope of its intent. |
| DEP-10 | FIXED | `worker/backfill_metadata.py:1-11` — new header docstring: states the script is unreferenced by any Dockerfile/compose/beat-schedule, predates the current metadata/tags flow, and gives the manual invocation `docker compose exec worker python backfill_metadata.py`. | Wave 5 Task 2 (commit `55964e9`, on `r10/wave5-sweep`) | Chose "add a doc header" over deletion — an explicitly offered alternative in the original recommendation, not a deviation. `print()` calls deliberately left as-is with a comment explaining why (interactive operator script, not a Celery task under the BP-19 structlog convention). |

## Regressions/Surprises

None found. All 10 findings are FIXED at HEAD, independently re-verified (not
parroted from wave reports):

- DEP-1, DEP-2, DEP-6 (partially — the dead-export half) landed in waves 1/4 and are
  already merged to `main`.
- DEP-3, DEP-4, DEP-5, DEP-7, DEP-9, DEP-10, and DEP-6's `createGroupChat` adoption
  landed in Wave 5 (commits `a9fc47d`, `e49a50c`, `d646791`, `fc484bf`, `55964e9`) —
  present on `r10/wave5-sweep` at `764777c` but **not yet merged to `main`** as of
  this verification (branch-local only; expected, since this is the in-flight sweep
  branch itself).
- DEP-8's two halves (lint-debt-zero + CI gate) both independently confirmed by
  re-running `ruff check .` (api/worker/shared, all clean) and `npm run lint`
  (frontend, 0 problems) myself rather than trusting the wave reports' cited output.
- One scope-expansion worth flagging (not a regression): DEP-5's sweep deleted 5
  additional zero-read Settings fields (`embed_provider`, `ollama_embed_model`,
  `ollama_vision_model`, `openai_api_key`, `voyage_api_key`) beyond the finding's
  named four. The wave-5 report explicitly declined to also delete
  `anthropic_api_key`/`claude_model`/`llm_provider`/`ollama_llm_model` (same
  zero-`settings.X`-read pattern) citing scope discipline — flagged there as a
  follow-up dep-hygiene item for a future pass, not something this verification
  needs to act on.
