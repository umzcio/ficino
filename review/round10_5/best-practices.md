# R10.5 Verification — BEST PRACTICES (BP-1 … BP-19)

Verified at HEAD `764777c` (branch `r10/wave5-sweep`), all five waves merged. Every
row below is evidence I personally read at the cited file:line — no finding was
taken on faith from `review/round10/best-practices.md` or from `progress.md`.

| ID | Status | Evidence (file:line) | Wave | Notes |
|----|--------|----------------------|------|-------|
| BP-1 | FIXED | `api/services/llm.py:22-` (`llm_error_to_http`, graded 504/503/502/400/500); `api/routers/replies.py:570` (`create_reply` now calls it) and `:815` (`zap_response`); `api/routers/personas.py:248` (`send_persona_dm`) | W3 (Task 10) | Shared helper extracted exactly as recommended; all three call sites converged, event-name kwarg preserves per-site structlog labels. |
| BP-2 | FIXED | `api/routers/feed.py:373` (feed-not-found → 404), `:381` (post-index-out-of-range → 404, comment cites BP-2 alignment); `api/routers/personas.py:305` (404 "Message index out of range"); `api/routers/replies.py:687` (404 "Message not found") | W3 (Task 10) | `feed.py` was the outlier (400); now aligned to 404 across all three peers. |
| BP-3 | FIXED (with documented exceptions) | Pattern-A converged: `api/routers/likes.py:87-101` (204+404), `bookmarks.py:97-116` (204+404), `alerts.py:88-` (`dismiss_alert` now 204+404, comment cites BP-3 rewrite from prior `200 {"status":"ok"}`). Deliberately-kept idempotent composite-key deletes, each with an inline rationale: `bookmarks.py:120-153` (`delete_bookmark_by_post`), `tags.py:133-148`, `workspaces.py:165-181`. `personas.py:322-360` (`clear_persona_dm`) kept `200 {"messages":[...]}` intentionally, documented at `:337-346` for a real frontend consumer. | W3 (Task 10) | Not a blanket single-contract fix — three shapes remain, but each is now labeled and justified in-code rather than accidental drift. This matches the original recommendation's own "or document the intentionally-idempotent ones" fallback. |
| BP-4 | FIXED | `api/models/requests.py:1-3` (new file, "Promoted from ~11 routers (R10 BP-4 promote half)"); all 14 routers now `from models...` import (`likes.py:10`, `feed.py:17`, `user_posts.py:16`, `personas.py:15`, `papers.py:19`, `annotations.py:11`, `reading_lists.py:16`, `users.py:10`, `bookmarks.py:13`, `tags.py:10`, `workspaces.py:13`, `messages.py:16`, `replies.py:17`, `settings.py:15`); dead models `Chunk`/`Figure`/`PaperCreate` removed from `api/models/paper.py` (now only `PaperTag`+`Paper`, both live) | W3 (Task 11) | `api/tests/test_models_convention.py` locks in the promote-half convention as a repo-wide test. |
| BP-5 | FIXED | `api/routers/reading_lists.py:324-336` (`apply_ai_ordering(list_id, body: ApplyOrderingRequest, ...)`, comment cites "R10 API-12/BP-5") | W3 (Task 11) | Raw `dict` replaced with a proper pydantic model; 422 now fires on malformed input instead of a raw 500. |
| BP-6 | FIXED | `api/config.py:50-51` (`rate_limit_replies_per_day: int = 60`, `rate_limit_persona_dm_per_day: int = 60`); `api/routers/replies.py:350,711` and `personas.py:157` now read `settings.rate_limit_*_per_day`; `api/config.py:56` comment confirms `generation_limit_per_day` deleted | W1/W3 | Dead knob removed, both hardcoded 60s promoted to env-overridable Settings fields. |
| BP-7 | FIXED | `shared/ficino_shared/settings_schema.py:1-60` (`DEFAULTS` dict, single source, all three defaults keyed off `os.getenv`); `api/routers/settings.py:17` and `worker/lib/settings.py:17-18` both `from ficino_shared.settings_schema import DEFAULTS` | W2 (DUP-1) | Triplication fully collapsed into one shared module both containers import; `api/config.py:20-27` keeps its own Settings-class mirror only for the two provider-selection fields still genuinely api-local (documented at config.py:22-27). |
| BP-8 | PARTIAL | Cross-service portion done: `shared/ficino_shared/constants.py:12-13` (`SIGNED_URL_DEFAULT_TTL=600`, `MEDIA_URL_TTL=86400`), `CHAPTER_INSERT_SQL` also moved there. Single-service literals still scattered, unconsolidated: HTTP timeouts `300.0`/`120.0` still repeated verbatim (`worker/lib/claude_client.py:62`, `vision_extractor.py:134`, `figure_detector.py:265`, `contextualizer.py:85`, `embedder.py:56,132`, `api/services/llm.py:93,182`); retry backoff `range(3)`/`2*(3**attempt)` still duplicated (`worker/lib/embedder.py:61,81`, `claude_client.py:60,91`); session TTL `604800` still duplicated (`api/auth/providers.py:64,85`); `top_k` still scattered as bare literals across `persona_tasks.py:397,611,975`, `archivist_tasks.py:209,354`, `reading_list_tasks.py:322` | W2 | Recorded rationale for the partial scope: wave-2 plan (`docs/superpowers/plans/2026-07-02-r10-wave2-shared-package.md:13`) explicitly states "Resolves: ... BP-8 (**cross-service portion**)" — the single-service timeout/backoff/top_k literals were knowingly left out of scope, not missed. |
| BP-9 | FIXED | `frontend/src/auth/AuthContext.tsx:3` (`import { setAuthTokenGetter, request, ApiError, getApiErrorDetail } from '../lib/api'`); `:217,246,424` now call `request('/auth/...')` instead of raw `fetch` | W4 | `frontend/src/lib/api.test.ts:1` locks in `getApiErrorDetail` as the shared helper AuthContext consumes; duplicate `API_BASE` and the 7 raw fetches are gone. |
| BP-10 | FIXED | `api/tests/test_audit_coverage.py:1-16` (dedicated regression test, both behavioral — danger-zone clear + the exact `delete_bookmark_by_post` gap BP-10 called out — and a static sweep); `record_audit` now present in `workspaces.py:185`, `user_posts.py:224`, `personas.py:317,355`, `likes.py:105`, `settings.py:241,259,274,334,405` (the five danger-zone clears), `replies.py:694`, `bookmarks.py:113,145` (both delete paths) | W3 (Task 12) | Comment at `bookmarks.py:141-144` explicitly notes both delete-bookmark routes now share one `"bookmark.delete"` audit action so which path you took no longer matters. |
| BP-11 | PARTIAL | Primitives relocated: `frontend/src/components/_shared/primitives.tsx` (was `Settings/primitives.tsx`). `Slider` adopted outside Settings: `frontend/src/components/Listen/ListenView.tsx:9,802,843` (`import { Slider } from '../_shared/primitives'`). `DangerButton` still narrowly consumed — only `frontend/src/components/Settings/StorageTab.tsx` imports it; `frontend/src/components/Feed/PostCard.tsx:147,494-500` and `UserPostCard.tsx:34,92,155-158` still hand-roll their own `confirmingDelete` click-twice-within-a-window state, not converged onto `DangerButton` | W4 | The finding's headline complaint (sliders reinvented in ListenView) is fixed and the file moved to the shared location as recommended. The card-shell/ghost-pill/`DangerButton` consolidation named in the same finding was not carried through — three destructive-confirm UX patterns likely still coexist (not independently re-verified beyond the two PostCard sites above). |
| BP-12 | FIXED | `api/routers/users.py:18-20` (`db: asyncpg.Connection = Depends(get_db)`, typed, matching every other router); `:19-31` (`-> dict[str, object]` return annotations on both profile endpoints); `:27` (`HTTPException(status_code=404, detail="User not found")`, keyword args) | — | All three convention gaps (untyped db, missing annotations, positional HTTPException args) closed; file now matches house style. |
| BP-13 | FIXED | `api/routers/personas.py:22-24` (`GET /personas` now `Depends(get_current_user)`); `api/routers/settings.py:191-199` (`GET /settings/ollama-models`, comment: "Auth-gated (R10 API-6/BP-13): was previously reachable by anyone") | W3 | Both previously-unauthenticated endpoints gated; the settings.py one also carries an explanatory comment as the finding's fallback recommendation suggested. |
| BP-14 | FIXED | `api/config.py` now has `search_use_normalized_posts` on `Settings` (read at `api/routers/search.py:96,158` via `settings.search_use_normalized_posts`, comment at config.py:88 cites BP-14); `api/textutil.py:1-9` (new shared module, `escape_like` public, comment explains the underscore-private cross-router import it replaces); `api/routers/replies.py:19` and `search.py:13` both `from textutil import escape_like` | W3 | Both sub-issues (bare `os.getenv`, private cross-router import) resolved. |
| BP-15 | FIXED | `api/celery_client.py` (new shared module, `get_celery()`); imported identically by `feed.py:11`, `messages.py:10`, `user_posts.py:10`, `reading_lists.py:10`, and now also `papers.py:13` (previously the inline-Celery outlier); grep for `_get_redis`/inline `Celery(` in `papers.py` returns nothing — dead Redis client removed | — | All five dispatch sites converged on one helper; the dead per-upload Redis connection is gone. |
| BP-16 | FIXED | `api/constants.py` (current contents: only `DEFAULT_WORKSPACE_ID`/`STUB_USER_ID` re-export + the BP-18 LIMIT constants) — `ENGAGEMENT_RANGES` no longer present; `worker/tasks/persona_tasks.py:38-39` ("across containers — and was removed, R10 BP-16") keeps the sole live copy | — | Dead mirror with the false "source of truth" comment deleted; worker copy is now honestly the only one, with a comment noting the api-side twin was removed. |
| BP-17 | FIXED | `shared/ficino_shared/constants.py:18-30` (`CHAPTER_INSERT_SQL`, batched `unnest WITH ORDINALITY`); `api/routers/reading_lists.py:231` (create, uses `CHAPTER_INSERT_SQL`), `:40-46,317` (`REORDER_CHAPTER_INSERT_SQL`, reorder — also batched, single `db.execute`, comment at `:307-313` explicitly cites BP-17 and explains why reorder needs its own sibling SQL rather than reusing the create-path statement verbatim), `:385` (`apply_ai_ordering` reuses `CHAPTER_INSERT_SQL` directly) | W3 (Task 11) | All three sites now single round-trip; reorder's divergent SQL is deliberate (doesn't reset progress) and documented, not a missed consolidation. |
| BP-18 | FIXED | `api/constants.py:11-19` (`MAX_FEEDS_LIST=20`, `MAX_CONTENT_LIST=50`, `MAX_ALERTS_LIST=50`, `MAX_ACTIVITY_FEED=100`, `MAX_PAPER_SUMMARIES_LIST=200`, `MAX_LIBRARY_LIST=500`, `MAX_ANNOTATIONS_LIST=1000`, `MAX_SEARCH_RESULTS=10`, `MAX_SEARCH_CHUNKS=15`, each with an inline comment naming every consuming file) | W5 (Task 1, per BP-18 comment block) | All nine previously-bare LIMIT literals now named constants in one file, discoverable and tunable in one place; matches the recommendation exactly (did not additionally add a universal pagination idiom, which the finding treated as optional). |
| BP-19 | FIXED | `logger.warn(` count across `api/`+`worker/` = 0 (`.warning(` count = 24 files); `worker/backfill_metadata.py:9-11` now explicitly documents its `print()` usage as intentional ("this is an operator script, and printing directly off the terminal is the point (R10 BP-19)"); `worker/tasks/ingestion_tasks.py:86-90` documents the `log = logger.bind(...)` convention as deliberate, not drift (comment cites BP-19) | W5 (sweep) | Verified myself via `grep -rn "logger\.warn("` (zero hits) — the deprecated alias is fully swept, not just reduced. All three BP-19 sub-issues (`.warn` alias, `logger`-vs-bound-`log` split, bare `print()`) now resolved or explicitly documented as intentional. |

## Regressions/Surprises

None found. Every one of the 19 findings improved or held; zero regressions
detected across any of BP-1–BP-19.

Two items are usefully imprecise if read as flat "FIXED" without the caveat:

- **BP-8** is genuinely only half-done — and that's by design, not neglect.
  The wave-2 plan text itself scopes it to "cross-service portion," so the
  remaining single-service timeout/backoff/top_k literals (still scattered
  across `worker/lib/*.py` and `worker/tasks/*.py`) are an accepted, recorded
  deferral rather than a missed cleanup.
- **BP-11** is a real partial: the specific bug in the original finding
  (ListenView hand-rolling sliders) is fixed and the primitives file did move
  to `components/_shared/` as recommended, but the broader "converge all
  card-shell/ghost-pill/destructive-confirm reinventions" ask was not carried
  through — `PostCard.tsx`/`UserPostCard.tsx` still have their own
  `confirmingDelete` pattern distinct from `DangerButton`.
