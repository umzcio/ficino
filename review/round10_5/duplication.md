# R10.5 Verification — DUPLICATION (review/round10/duplication.md)

Verified at HEAD `764777c` (branch `r10/wave5-sweep`, all five remediation waves present).
Method: two parallel verification passes (backend DUP-1..7,12..18; frontend DUP-8..11,19,20), every
status-determining claim independently re-verified by the controller via direct grep/read at HEAD.
Wave attribution from `.superpowers/sdd/progress.md` (commit hashes preferred over ledger prose).

**Counts: 13 FIXED, 5 PARTIAL, 2 DEFERRED, 0 SUPERSEDED, 0 REGRESSED.**

| ID | Status | Evidence (file:line) | Wave | Notes |
|----|--------|----------------------|------|-------|
| DUP-1 | **FIXED** | Shared impl: `shared/ficino_shared/settings_schema.py:20-118` (DEFAULTS, SETTINGS_TO_ENV, UI_ONLY_KEYS :91, `reassert_public_deployment` :237-259). Consumers: `api/routers/settings.py:17-25` imports schema; `ALLOWED_SETTINGS_KEYS = frozenset(DEFAULTS.keys())` :58; reassert now in api GET/PUT paths :83-88, :182-187. `worker/lib/settings.py:16-25` imports same schema. `api/services/llm.py:17` reads `_SCHEMA_DEFAULTS`. | W2 T6 (9220857) + 554e091 | All 3 original divergences closed; PUBLIC_DEPLOYMENT reassert now on both services. 554e091 further hardened worker reassert to read the frozen operator `_baseline_env` snapshot (not live env), pinned by `test_public_deployment_does_not_launder_previous_users_key`. |
| DUP-2 | **FIXED** | Shared impl: `shared/ficino_shared/storage/{base,local,supabase}.py`. Shims: `worker/lib/storage.py:1-14` (`build_backend(...)`), `api/storage/__init__.py` same pattern. Traversal defense present and strengthened: `shared/ficino_shared/storage/local.py:85-93` — `resolve()` + `relative_to(base)` on both `paper_id` and `filename` (original api copy checked only the final path; worker checked nothing). | W2 T7 (684b30d+234301e) | Residual (non-duplication): `read_figure_bytes` remains zero-production-caller dead code (only `shared/tests/test_storage_local.py` calls it; `api/routers/figures.py:62` serves via FileResponse). W2 T7 ledgered it as a "wave-5 dead-code candidate" but W5's deletion sweep did not remove it — open R10.5 item. |
| DUP-3 | **FIXED** | Shared impl: `shared/ficino_shared/sanitize.py` (`strip_role_markers` :38, `fence_untrusted` :52, `sanitize_inline` :73). Shims: `api/sanitize.py:1-11` ("Shim — the implementation lives in ficino_shared.sanitize (R10 DUP-3)", re-exports incl. `strip_role_markers` and `_MAX_BLOCK_LEN`); `worker/lib/sanitize.py:1-6` same. | W2 T2 (49c42ca+b144e84) | `fence_lines` (worker-only helper the finding said api lacked) was proven zero-caller and deliberately **deleted** (903050a, R10 WORK-16) rather than ported — premise dissolved by deletion. |
| DUP-4 | **FIXED** | Shared impl: `shared/ficino_shared/signed_url.py` (`sign_resource` :52, `verify_token` :61). Shims: `api/signed_url.py:1-9` and `worker/lib/signed_url.py:1-9` ("Shim — the implementation lives in ficino_shared.signed_url (R10 DUP-4)"), both re-exporting `DEFAULT_TTL_SECONDS/sign_resource/verify_token`. | W2 T3 (b7faca8) | Stale "24h" docstring gone; shared docstring now correctly documents the api short-TTL vs worker 30-day figure-TTL split. Zero "24h" matches remain. |
| DUP-5 | **PARTIAL** | Loop helper half FIXED: `worker/lib/event_loop.py:11` `LoopRunner` (run_coroutine_threadsafe pattern) adopted by all 7 surviving modules — `db.py`, `claude_client.py`, `embedder.py`, `contextualizer.py`, `figure_detector.py`, `vision_extractor.py`, `metadata_extractor.py:14,117-124` (migration comment documents the old run_until_complete-under-lock regression as eliminated). 8th copy gone because `worker/lib/figure_describer.py` was deleted as dead code (a9fc47d, R10 WORK-10). `_get_config` half NOT consolidated: 5 per-module readers remain (claude_client, embedder, contextualizer, figure_detector, vision_extractor), now thin wrappers over shared `get_active()`/`ollama_base_url()` primitives — `worker/lib/settings.py:56-64` documents this as a scoped "narrow consolidation" decision. | W3 T5 (0219ab4) | The serialization regression (metadata_extractor) — the finding's live-bug core — is fixed. The `_get_config` residue is a recorded scope decision, not an oversight, but the finding's second half is by definition unconsolidated → PARTIAL. |
| DUP-6 | **FIXED** | `worker/lib/persona.py:140` `build_post_sources()` (docstring cites R10 DUP-6). All 4 former sites call it: `worker/tasks/persona_tasks.py:773`, `:1006`, `worker/tasks/reading_list_tasks.py:374`, `worker/tasks/archivist_tasks.py:251`. | W3 (commit 2bcdddc) | Ledger prose files this commit under W3 T6 (which describes DUP-7); commit title is authoritative ("R10 DUP-6, DUP-12"). |
| DUP-7 | **PARTIAL** | Behavioral convergence only — no shared module. `api/services/llm.py:92-134` `_post_ollama_chat_with_retry` and `worker/lib/claude_client.py:37-98` `_generate_ollama` remain two independent implementations with now-identical semantics (3 attempts, 2s/6s/18s backoff, 5xx-only retry, matching empty-response guards); cross-reference tags "R10 DUP-7a" at `llm.py:99` / "DUP-7b" at `claude_client.py:81`. `shared/ficino_shared/` contains zero LLM/retry code. | W3 T6 (2bcdddc, c117fda, 53263ae) | Review during W3 T6 caught a 4xx-retried defect faithfully ported from the worker and fixed it on both sides with regression pins. But parity is comment-enforced only — a future retry-timing edit still needs two synchronized edits. Most drift-prone survivor; candidate for a shared retry helper. |
| DUP-8 | **FIXED** | `frontend/src/lib/timeAgo.ts` exists (+ `timeAgo.test.ts`). All 6 original sites import it: `AlertsView.tsx`, `FeedHistory.tsx`, `ExploreView.tsx`, `BookmarksView.tsx`, `Inbox.tsx:17-22` (thin wrapper, comment cites DUP-8), `UserPostCard.tsx`. The 7th straggler ledgered by W4 T1 (`StorageTab.tsx formatAgo`) is gone: `StorageTab.tsx:7,65` now imports shared `timeAgo` (commit 3c43a5c, wave 5). `AccountTab.tsx` also adopted it. | W4 T1 (98c12d2) + W5 (3c43a5c) | Anyone reading only the W4 ledger note would mis-report PARTIAL — the straggler was resolved one wave later. Fully fixed at HEAD, no remaining copies. |
| DUP-9 | **FIXED** | `frontend/src/components/_shared/AsyncState.tsx` exports `Spinner`/`EmptyState` (header comment cites "R10 DUP-9"). All 7 named views import it: `BookmarksView.tsx`, `AlertsView.tsx`, `ReadingListsView.tsx`, `Inbox.tsx`, `SettingsView.tsx`, `ExploreView.tsx`, `ReadingListDetail.tsx` — plus new adopters (`NewGroupChatModal.tsx`, `AccountTab.tsx`). | W4 T1 (3cb5778) | New components are adopting the primitive rather than re-rolling — the consolidation is holding. |
| DUP-10 | **PARTIAL** | (1) Sources-reveal FIXED: `Feed/_shared/SourcesList.tsx` exists; consumed at `PostCard.tsx:14,729` and `UserPostCard.tsx:10,337`. (3) Avatar FIXED: `Feed/_shared/Avatar.tsx:10` takes `size = 42` prop, DUP-10 fix documented at :3-9; `UserPostCard.tsx:321` uses `<Avatar persona="archivist" />` (hand-rolled 40px/1.5px copies gone, normalized to shared 42px). (2) Straggler: persona name/handle header button pair still verbatim duplicated — `PostCard.tsx:403-413` vs `UserPostCard.tsx:325-330` (same `hover:underline` button pair, same onClick shape). | W4 T3 (ec52e0a) | The recommendation only named SourcesList + Avatar (both done cleanly; visual check during W4 even caught a flex-stretch bug pre-ship). The header-button block was in the finding body but never given a remediation target — it is the remaining duplicate site. |
| DUP-11 | **DEFERRED** (5/7 fixed; remainder approved deferral) | `frontend/src/hooks/usePollTask.ts` exists (+ test). Adopters: `useFeed.ts:5,93` ("R10 DUP-11: canonical usePollTask"), `useUserPosts.ts:4,51`, `PaperChat.tsx`, `ReadingListDetail.tsx:14,31,68,92` (both loops), `UserPostCard.tsx`; new adopter `GroupChatView.tsx` (W4 T9/W5 T4). Deferred: `ListenView.tsx`'s two pollers still hand-rolled (`pollUntilFeedAudioReady` :290-338, podcast poller below), coupled through a single `activePollerRef` mutex (:93,143,295-335) preventing a stale poller from stomping the other mode's timer. | W4 T2 (6aea6ab, ce23f58) | Recorded rationale (progress.md W4 T2): "ListenView's 2 pollers deferred on legitimate mutex/per-tick grounds — R10.5 note: reevaluate with onTick support". Deferral is substantiated in code, not just asserted: usePollTask's per-call-independent design genuinely doesn't fit the shared-mutex coupling without an extension. |
| DUP-12 | **FIXED** | `worker/lib/persona.py:164` `resolve_enabled_personas()` called from `persona_tasks.py:312` and `reading_list_tasks.py:313` (comment at :312 cites R10 DUP-12). `worker/tasks/archivist_tasks.py:31` `_get_paper_ids()` called from both scoping sites :193 and :341. | W3 (commit 2bcdddc) | Same commit as DUP-6; ledger prose mislabels it under T6. |
| DUP-13 | **FIXED** | `api/celery_client.py` exists (module-level `Celery(broker=..., backend=...)` + `get_celery()`; docstring names papers.py's old missing-`backend=` drift). All 5 routers import it: `feed.py:11`, `user_posts.py:10`, `reading_lists.py:10`, `messages.py:10`, `papers.py:13`. No inline stragglers. | W3 T7 (e001468) | papers.py's missing-backend drift closed as a side effect. |
| DUP-14 | **PARTIAL** | Fixed halves: `shared/ficino_shared/constants.py` holds `STUB_USER_ID`/`DEFAULT_WORKSPACE_ID`; `api/constants.py:3` re-imports (no redefinitions); `worker/lib/settings.py:16` imports `STUB_USER_ID` from shared. Frontend `useWorkspaces.ts:8` mirror is cross-referenced by comment at `shared/.../constants.py:6-9` (per recommendation — TS can't import the Python package). Pool env collision resolved: `worker/lib/db.py:39-40` uses `WORKER_DB_POOL_MIN/MAX_SIZE` with fallback (comment cites R10 DUP-14). Straggler: DSN default `postgresql://ficino:ficino@postgres:5432/ficino` still duplicated at `api/config.py:7`, `worker/lib/db.py:17`, `api/tests/conftest.py:29`, `worker/tests/conftest.py:17`, `infra/postgres/migrate_personas_v3.py:12`, `infra/postgres/backfill_feed_posts.py:25`. | W3 T1 (79cad34) | The DSN-default sub-finding was neither fixed nor recorded as a deferral anywhere in progress.md — the only silently-skipped sub-item found in this area. Low risk (dev-only default) but unledgered. |
| DUP-15 | **FIXED** | Dead api copy deleted: `api/constants.py` (24 lines) contains only LIMIT caps + shared re-imports; repo-wide grep finds `ENGAGEMENT_RANGES` only at `worker/tasks/persona_tasks.py:39-47` (sole copy, consumed at :49). False "source of truth" comment gone. | W5 T1 (72cf6b7) | |
| DUP-16 | **FIXED** | `api/routers/papers.py:33` `_paper_from_row()` used at :176, :214, :234; the two list-SQL variants collapsed into one query with `($2::uuid IS NULL OR p.corpus_id = $2)` (:193-208). `api/routers/feed.py:30` `_feed_from_row()` used at :252 (detail) and :493 (list, `include_media=False`). | W3 T8 (56c048f) | Exactly the recommended shape, including the conditional-predicate SQL merge. |
| DUP-17 | **FIXED** | `api/routers/replies.py:177` `_load_conversation_and_sources()` (docstring: "Shared by create_reply and zap_response (R10 DUP-17)"); called from `create_reply` at :366 and `zap_response` at :740. | W3 T7 (520c06c) | |
| DUP-18 | **FIXED** | `shared/ficino_shared/constants.py:23-30` `CHAPTER_INSERT_SQL` imported and executed as the live query by both services: `api/routers/reading_lists.py:15,231,385` and `worker/tasks/reading_list_tasks.py:17,210`. Identity pinned by `worker/tests/test_shared_adoption.py:8` (`is`-assertion). | W2 T1 (e20c81e); docstring rider W3 T12 (d8ff7e1) | Went beyond the original "cross-ref comments now, shared constant later" — the shared SQL constant landed and is the executed statement. Reorder deliberately uses documented sibling `REORDER_CHAPTER_INSERT_SQL` (`reading_lists.py:26-40`) because its progress-reset semantics differ — not a miss. |
| DUP-19 | **PARTIAL** | `Md` promoted to `frontend/src/components/_shared/Md.tsx`; the flagged violation is fixed — `PersonaProfile.tsx:7` now imports `{ Md } from '../_shared/Md'` (same-tier). But `InlineMd` was NOT promoted (still `Feed/_shared/InlineMd.tsx`), and the promoted file itself now reaches back across the boundary: `components/_shared/Md.tsx:7` imports `{ InlineMd } from '../Feed/_shared/InlineMd'`. | W4 T1 (5a6e6ea) | The recommendation said promote "Md (and InlineMd if needed)" — half-done. The fix relocated rather than eliminated the cross-boundary import; commit message for 5a6e6ea is candid that this was a deliberate scope cut. Fresh instance of the flagged pattern, inside the shared folder itself. |
| DUP-20 | **DEFERRED** (opportunistic-only by original rating; 8/9 sub-items unchanged, 1 mooted) | Spot-checked all 9: App.tsx PersonaProfile JSX still duplicated (:611-621 vs :643-653, now with justifying comments); AuthContext verifyOtp/updateUser 3-way drift persists (:308-337/:339-360/:362-390); useKeyboardAwareInput handlers still near-identical (:14-42 vs :51-74); offline-cache put-and-prune still repeated (cacheFeeds :7-27 vs cachePapers :44-60 + more stores); WorkspaceBottomSheet:86-98 vs WorkspaceDropdown:151+ still duplicated; GroupChatView:250-256 vs PaperChat:190-196 sticky header still identical; `worker/lib/db.py` try/rebuild-pool wrapper still ×4 (:74-86, :89-101, :104-116, :119-129); figure_describer/vision_extractor pair MOOTED by figure_describer's deletion (a9fc47d); test_idor_followups preamble still duplicated (:145-161 vs :182-194). | — | Original finding rated "fix opportunistically"; no wave scheduled it and none was claimed. No recorded deferral entry in progress.md, but the original report's own recommendation is the de-facto rationale. The db.py wrapper and App.tsx block remain the two most worthwhile targets. |

## Regressions / Surprises

**REGRESSED: none.** No consolidated code has re-forked; new frontend components (`AccountTab`, `NewGroupChatModal`, `GroupChatView`) are adopting the shared primitives (`timeAgo`, `AsyncState`, `usePollTask`) rather than copying — the consolidations are holding under new development.

Surprises (none rise to REGRESSED, all worth carrying):

1. **DUP-19's fix created an inverted instance of the flagged pattern.** The PersonaProfile→Feed/_shared import was fixed by promoting `Md`, but the promoted `components/_shared/Md.tsx:7` now imports `Feed/_shared/InlineMd` — a shared-tier file reaching down into a feature folder. Deliberate scope cut per the commit message, but the boundary problem the finding named is only half closed.
2. **DUP-14's DSN-default duplication was silently skipped** — still in 6 places; the only sub-finding in this area neither fixed nor recorded as a deferral anywhere in progress.md.
3. **`read_figure_bytes` dead-code deferral slipped through wave 5** (DUP-2 residual). W2 T7 explicitly ledgered it as a wave-5 dead-code candidate; the W5 deletion sweep didn't remove it. Still zero-production-caller at HEAD.
4. **DUP-7's "true convergence" (ledger wording) is behavioral, not structural** — two hand-synced retry implementations with cross-reference comments; nothing enforces parity. Most drift-prone survivor.
5. **Ledger prose mislabels the DUP-6/DUP-12 commit** — progress.md files 2bcdddc's work under W3 T6 text that describes only DUP-7; the commit title ("R10 DUP-6, DUP-12") is authoritative. Trust hashes over prose when auditing this campaign.
6. **`worker/lib/figure_describer.py` no longer exists** (deleted as dead code, a9fc47d, R10 WORK-10) — DUP-5's "8 copies" is legitimately 7-all-consolidated, and one DUP-20 sub-pair is mooted. Not a regression.
7. **DUP-8's W4 ledger note went stale within the campaign**: the "7th timeAgo helper" (StorageTab `formatAgo`) it flagged was fixed in wave 5 (3c43a5c) — HEAD is fully FIXED despite the ledger reading as PARTIAL.
