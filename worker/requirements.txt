celery>=5.4.0
redis>=5.2.0
asyncpg>=0.29.0
anthropic>=0.40.0
openai>=1.6.0
pymupdf>=1.24.10
structlog>=24.1.0
Pillow>=10.3.0
tiktoken>=0.7.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.28.0
# Local cross-encoder reranker (RERANK_PROVIDER=local). Pulls torch; only
# imported when the local provider is actually used. Hosted providers
# (voyage / cohere) don't need this — but it's in the base image so a
# flip of RERANK_PROVIDER doesn't require a rebuild.
sentence-transformers>=3.0.0
