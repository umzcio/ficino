celery>=5.4.0
redis>=5.2.0
asyncpg>=0.29.0
anthropic>=0.40.0
openai>=1.6.0
pymupdf>=1.24.10
structlog>=24.1.0
Pillow>=10.3.0
tiktoken>=0.7.0
# Transitive via anthropic/openai SDKs — no direct `import pydantic` in
# worker code (worker/lib/settings.py is a hand-rolled os.getenv loader,
# not a BaseSettings class). Pin kept as a floor so both SDKs resolve to
# a version we've tested. R10 DEP-4: pydantic-settings removed — it was
# pure dead weight (not even transitively required).
pydantic>=2.9.0
httpx>=0.28.0
# Local cross-encoder reranker (RERANK_PROVIDER=local). Pulls torch + CUDA
# libs (~5GB); only used when the local provider is actually selected.
# Kept out of the base install so the default image is slim enough to boot
# reliably on PaaS runtimes. Self-hosters who want local reranking should
# `pip install -r requirements-rerank.txt` in their worker Dockerfile.
# sentence-transformers>=3.0.0

# Only imported when STORAGE_PROVIDER=supabase — safe to install on
# self-host builds too.
supabase>=2.8.0
